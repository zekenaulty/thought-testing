# Secrets
.env
.env.*

# Python
__pycache__/
*.py[cod]
.venv/

# Raw provider-native reasoning artifacts. Keep local: these may contain
# semantically recoverable hidden state.
bookforge-thought-corpus/capsules/
bookforge-thought-corpus/raw/
capsules/
raw/

# Probe outputs may also recover hidden state; keep local by default.
results/
